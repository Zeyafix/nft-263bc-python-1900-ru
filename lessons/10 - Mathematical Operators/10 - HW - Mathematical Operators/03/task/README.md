# Задание 3. Исправьте сборку сообщения

Файл решения: `main.py`.

В файле [main.py](main.py) число уже преобразовано в `int`. Вычисление выполняется, но сборка сообщения вызывает ошибку.

Исправьте программу. Сохраните вычитание 3 и конкатенацию через `+`. Ввод — любое целое число, в том числе ноль или отрицательное.

Пример после исправления:

```text
Enter a number: 10
Result: 7
```
